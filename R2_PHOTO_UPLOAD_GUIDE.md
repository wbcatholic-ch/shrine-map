# 성지 사진 올리기

앞으로 새 성지는 아래 형식으로만 올립니다.

```
shrines/
  성지의 앱 표기명/
    01.jpg
    02.jpg
    photos.json
```

- 폴더명은 앱에 표시된 성지명과 같게 씁니다. 공백이 있는 이름도 그대로 사용합니다.
- 사진 파일은 `01.jpg`, `02.jpg`처럼 두 자리 순서로 이름을 붙입니다.
- `photos.json`은 마지막 항목 뒤에 쉼표를 넣지 않습니다.

```json
{
  "photos": [
    {"file": "01.jpg", "portrait": false},
    {"file": "02.jpg", "portrait": true}
  ]
}
```

`portrait`는 세로 사진일 때만 `true`로 지정합니다. 사진을 더 추가하거나 순서를 바꾸면 앱은 다음 자료 화면을 열 때 갱신된 목록을 읽어 같은 페이드 갤러리로 표시합니다.
